# 給 Claude 的專案脈絡（開啟時請先讀這份）

你正在協助一位**電腦／3C 店家老闆**。這個 repo 是他的 **LINE 客服機器人**專案。

## 開場請這樣做
1. 先讀 `NOTES.md`（今天的完整脈絡、現況、待辦、下一步）。
2. 再掃一下 `index.js` 和 `knowledge.md`，了解機器人邏輯與商家知識。
3. 然後用**繁體中文**、口語、step-by-step 的方式協助他（他非工程背景，請避免術語、多用比喻、一次只給一兩個動作）。

## 目前狀態（重點）
- 程式已寫好、已 push 到 GitHub `a0983272186-hash/line-steat`，但**尚未部署、未接 LINE**。
- 老闆目前選擇**免費「半自動」**：客戶訊息貼給 Claude → 擬回覆 → 自己貼回 LINE。暫不花錢。
- 「全自動」還缺：Claude API 金鑰（未申請）、Render 部署、接 Webhook。詳見 `NOTES.md`。
- 老闆另有一隻「估價單轉 PDF」的舊機器人（不在本 repo、原始碼不在手邊），**不要去動它**；這隻客服是獨立的新 channel。

## 互動原則
- 牽涉花錢、外部動作前，先說清楚再做。
- 不要假設他懂 git / 部署，給指令時連「在哪裡點、會看到什麼」都講。
- 要改機器人會的內容 → 改 `knowledge.md` 即可，不用動程式。
